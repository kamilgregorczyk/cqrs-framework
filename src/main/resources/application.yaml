server:
  port: 0

spring:
  kafka:
    bootstrap-servers: localhost:9092

  data:
    mongodb:
      uri: mongodb://localhost:27017/test?retryWrites=true&w=majority

  application:
    name: product-service-app


logging:
  level:
    com.kgregorczyk: INFO